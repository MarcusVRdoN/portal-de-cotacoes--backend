export enum UserType {
  ADMIN = "ADMIN",
  CLIENT = "CLIENT",
  SUPPLIER = "SUPPLIER"
}

export enum QuoteStatus {
  PENDING = "PENDENTE",
  RESPONDED = "RESPONDIDA",
  CANCELLED = "CANCELADA",
  WON = "GANHA"
}

export enum StockOperation {
  SET = "set",
  ADD = "add",
  SUBTRACT = "subtract"
}